<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="wrap">
      <ul>

        <% for (let item in result.rows) { %>

            <li>
              <h4><%= result.rows[item].title %></h4>
              <div><%= result.rows[item].body %></div>
            </li>

        <% } %>

      </ul>
    </div>
  </body>
</html>
